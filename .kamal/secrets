# Kamal secrets - pulled from environment variables
# DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.
#
# デプロイ前にサーバーで以下の環境変数を設定してください:
#   export KAMAL_REGISTRY_PASSWORD=<GitHub PAT with packages:write>
#   export SECRET_KEY_BASE=<bin/rails secret の出力>
#   export ATTR_ENCRYPTED_KEY=<openssl rand -hex 32 の出力>
#   export BLIND_INDEX_KEY=<openssl rand -hex 32 の出力>
#   export POSTGRES_PASSWORD=<openssl rand -base64 24 の出力>
#   export REDIS_PASSWORD=<openssl rand -base64 24 の出力>
#   export SMTP_ADDRESS=<SMTPサーバーアドレス>
#   export SMTP_USERNAME=<SMTP認証ユーザー>
#   export SMTP_PASSWORD=<SMTP認証パスワード>
#   export GITHUB_TOKEN=<GitHub PAT with repo read権限（pre-deployフック用）>

# GHCR registry
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails
RAILS_MASTER_KEY=$(cat config/master.key)
SECRET_KEY_BASE=$SECRET_KEY_BASE

# PII encryption
ATTR_ENCRYPTED_KEY=$ATTR_ENCRYPTED_KEY
BLIND_INDEX_KEY=$BLIND_INDEX_KEY

# Database
POSTGRES_PASSWORD=$POSTGRES_PASSWORD

# Redis
REDIS_PASSWORD=$REDIS_PASSWORD

# SMTP
SMTP_USERNAME=$SMTP_USERNAME
SMTP_PASSWORD=$SMTP_PASSWORD

# GitHub API（pre-deployフックでCI状態確認に使用）
GITHUB_TOKEN=$GITHUB_TOKEN
